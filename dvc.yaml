stages:
  update_common:
    cmd: "python -m notes.stages.update_common"
    deps:
      - "${paths.models}"
      - "${paths.stage_update_common}"
      - "${paths.common}"
      - "${paths.obsidian_common}"
      - "${paths.grad_text_expand_source}"
      - "${paths.personal_text_expand_source}"
    outs:
      - "${paths.grad_vscode}"
      - "${paths.grad_scripts}"
      - "${paths.grad_env}"
      - "${paths.personal_vscode}"
      - "${paths.personal_scripts}"
      - "${paths.personal_env}"
